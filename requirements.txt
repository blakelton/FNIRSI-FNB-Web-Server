# Core Flask
Flask>=3.0.0
Flask-SocketIO>=5.3.6
Flask-CORS>=4.0.0

# USB Communication
# hidapi is the primary USB transport (device/usb_reader.py). It uses the OS HID
# stack (IOHIDManager on macOS, hidraw on Linux, HID.dll on Windows), so it works
# on macOS without root -- unlike libusb, which cannot claim the meter's HID
# interface on macOS. pyusb/libusb1 are still used by the standalone scripts
# (fnb48p_monitor.py, fnb48p_server.py, simple_server.py).
hidapi>=0.14.0
pyusb>=1.2.1
libusb1>=3.1.0

# Bluetooth Communication
bleak>=0.20.0

# Data Processing
numpy>=1.26.0
pandas>=2.2.0

# Utilities
python-dotenv>=1.0.0
python-dateutil>=2.8.2

# System monitoring (optional, graceful fallback if missing)
psutil>=5.9.0

# Note: Do NOT install eventlet - it conflicts with Bluetooth asyncio
# Flask-SocketIO uses threading mode for compatibility
